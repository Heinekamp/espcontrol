# =============================================================================
# DEV CONFIG - Local development entry point (3.5" Waveshare portrait panel)
# =============================================================================
# Flash with: esphome run dev.yaml
# =============================================================================

substitutions:
  name: "espcontrol-waveshare-35"
  friendly_name: "EspControl Waveshare 3.5"
  espcontrol_component_url: "../.."
  espcontrol_component_ref: "HEAD"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

packages:
  espcontrol: !include packages.yaml

web_server:
  js_url: ""
  js_include: "../../docs/public/webserver/waveshare-esp32-s3-touch-lcd-35/www.js"
